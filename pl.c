#include<stdio.h>
int main(){
int i,size,sum=0;
printf("enter the value :");
scanf("%d",&size);
printf("enter the element");
for(i=0;i<=size;i++){
    scanf("%d",&a[i]);
}
sum=sum+a[i];
printf("sum of array is=%d ",sum);

}
